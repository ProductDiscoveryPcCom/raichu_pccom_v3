# Plan P3 (bugs funcionales) y P4 (polish UX) — Raichu v5.1.0

**Fecha:** 2026-04-07
**Scope:** Continuación de los sprints P0/P1/P2 ya aplicados (commit 4c83b39).
**Método:** Inspección dirigida del código + verificación de cada hallazgo.

P0/P1/P2 ya cubrieron: regex precompilado, `st.cache_resource`, scraping paralelo, validación CMS entre etapas, timeouts unificados, retry n8n, `st.status` con progress, before/after en rewrite, mensajes de error clasificados. Este plan NO repite ninguno.

---

## P3 — Bugs funcionales

### P3.1 — `min_score` ignora puntuaciones de 0 en correccion dual
**Severidad:** Alta — impacto en calidad
**Archivo:** `core/openai_client.py:346`

```python
min_score = min(claude_score, openai_score) if claude_score and openai_score else claude_score or openai_score
```

`claude_score and openai_score` evalúa False cuando cualquiera vale 0 (puntuación válida que indica "muy mal"). En ese caso cae al `or`, devolviendo el otro score en lugar del mínimo. Resultado: si Claude da 0 y OpenAI da 70, `min_score` = 70, perdiendo la señal crítica.

**Fix:** comparar contra `None` explícitamente.
```python
scores = [s for s in (claude_score, openai_score) if isinstance(s, (int, float))]
min_score = min(scores) if scores else 0
```

---

### P3.2 — `total_comments` siempre 0 al normalizar producto desde n8n
**Severidad:** Media — pérdida silenciosa de datos
**Archivo:** `utils/product_json_utils.py:238`

`_normalize_n8n_product()` hardcodea `"totalComments": 0` y luego `_build_product_data()` lee esa key (línea 270). Cualquier producto que venga por la ruta n8n llega al pipeline sin contador de reseñas, aunque el endpoint los exponga. Las mini-stories de reviews y los hints de credibilidad pierden señal.

**Fix:** leer `raw.get("total_comments", raw.get("totalComments", 0))` y propagarlo.

---

### P3.3 — `list(set(...))` pierde orden en frases IA y enlaces faltantes
**Severidad:** Baja — afecta reproducibilidad
**Archivo:** `core/openai_client.py:351, 356`

```python
all_ai_phrases = list(set(claude_ai + openai_ai))
all_missing = list(set(claude_missing + openai_missing))
```

`set()` no garantiza orden → cada ejecución produce un prompt de Stage 3 ligeramente distinto, dificultando A/B testing y reproducción de bugs.

**Fix:** `list(dict.fromkeys(claude_ai + openai_ai))` (preserva orden de inserción).

---

### P3.4 — Tabla de specs descarta filas con >2 columnas
**Severidad:** Media — pérdida de specs
**Archivo:** `utils/product_json_utils.py:118-119`

```python
parts = [p.strip() for p in line.split('|')[1:-1]]
if len(parts) == 2 and parts[0] and parts[0] != 'Clave':
```

Si la tabla markdown tiene una tercera columna (unidad, nota), la fila se descarta entera. Específicamente para n8n con tablas de tipo `| Clave | Valor | Unidad |`.

**Fix:** `if len(parts) >= 2 and parts[0] and parts[0] != 'Clave': specs[parts[0]] = ' '.join(parts[1:]).strip()`.

---

### P3.5 — Scraper duplicado en `ui/rewrite.py` no usa el de `core/scraper.py`
**Severidad:** Media — divergencia de comportamiento
**Archivo:** `ui/rewrite.py:2004-2049` vs `core/scraper.py`

`_scrape_single_url()` reimplementa scraping con headers propios, sin retry, sin las correcciones que recibió `core/scraper.py` (parallel scraping P0). El modo rewrite usa esta versión obsoleta → comportamiento inconsistente con modo new.

**Fix:** llamar a `core.scraper.scrape_url(url)` y eliminar la copia. Verificar paridad de campos retornados.

---

### P3.6 — Truncado fijo de 8000 chars en scraping de competidores
**Severidad:** Baja — pérdida de contexto
**Archivo:** `ui/rewrite.py:2035-2036`

Trunca a 8000 chars sin considerar el tamaño objetivo del rewrite. Para competidores largos (4-5k palabras) se pierden las secciones finales (FAQs, conclusiones), justo lo que más necesita el análisis.

**Fix:** elevar a 15000 o hacerlo proporcional al `target_length` del config.

---

### P3.7 — `puntuacion_general` devuelve 0 si es dict sin las keys esperadas
**Severidad:** Baja — degradación silenciosa
**Archivo:** `core/openai_client.py:340-345`

```python
if isinstance(claude_score, dict):
    claude_score = claude_score.get('general', claude_score.get('total', 0))
```

Si Claude devuelve `{"seo": 80, "tono": 75}` (sin `general`/`total`), el score se silencia a 0. El defensive code creado en commit 6c2b89c cubre el shape común pero no logea el shape inesperado.

**Fix:** loggear `logger.warning("puntuacion_general dict shape inesperado: %s", list(claude_score.keys()))` antes del fallback a 0.

---

### P3.8 — Validar que el archivo `VERSION` existe antes de leer
**Severidad:** Baja — robustez
**Archivo:** `version.py`

`version.py` lee `VERSION` con fallback a "5.1.0". Verificar que el fallback funciona si el archivo se borra accidentalmente y que no rompe imports en tests aislados (los tests pueden ejecutarse sin el archivo en CI Windows).

**Fix:** confirmar try/except cubre `FileNotFoundError` y `PermissionError`.

---

### P3.9 — Cache key de `_get_cached_generator` no incluye temperature/max_tokens
**Severidad:** Media — datos cacheados obsoletos
**Archivo:** `app.py` (revisar tras refactor a `@st.cache_resource`)

Si el usuario cambia `temperature` o `max_tokens` en sidebar, el generador cacheado puede no invalidarse. Verificar que la key del cache incluye TODOS los parámetros que afectan al output.

**Fix:** incluir todos los params en la clave del cache resource.

---

## P4 — Polish UX

### P4.1 — Indicador de modo activo en sidebar
**Esfuerzo:** S | **Archivo:** `ui/sidebar.py`
Sidebar lista 11 funcionalidades pero no resalta el modo actual. Añadir banner compacto al inicio: `**Modo activo:** 📝 Nuevo Contenido` que se actualiza con `st.session_state.mode`. Reduce confusión cuando el usuario navega entre paneles.

### P4.2 — Timestamp visible en panel de resultados
**Esfuerzo:** XS | **Archivo:** `ui/results.py:115-116`
"✅ Contenido Generado" no muestra cuándo. Añadir caption: `Generado el {fecha amigable}`. Útil al cambiar entre modos donde se restaura contenido viejo.

### P4.3 — Nombres de descarga descriptivos
**Esfuerzo:** XS | **Archivo:** `ui/results.py:271-277`
Pasar de `{keyword}_{YYYYMMDD}.html` a `{keyword}_{ARQ}_{stage}_{HHMM}.html`. En carpeta de descargas con 20 archivos, el contexto se pierde sin el arquetipo y la etapa.

### P4.4 — Spinners contextuales en `opportunities`
**Esfuerzo:** XS | **Archivo:** `ui/opportunities.py:164,849,861`
"Analizando..." genérico → "Analizando 145 keywords de GSC...", "Calculando scores de oportunidad...". Reduce la ansiedad de "¿se ha colgado?".

### P4.5 — `help=` en campos no obvios de rewrite
**Esfuerzo:** XS | **Archivo:** `ui/rewrite.py` (campos `keep_notes`, anchor text en merge)
Campos opcionales sin tooltip quedan ignorados. Añadir `help=` con 1 frase explicando qué entra y cómo se usa downstream.

### P4.6 — Confirmación + estimación de tamaño en "Descargar Todo"
**Esfuerzo:** S | **Archivo:** `ui/results.py:1930-1933`
Generar ZIP grande sin feedback frustra al usuario. Mostrar "📦 ZIP de 3 etapas (~XXX KB)" + spinner durante la generación.

### P4.7 — Estandarizar `label_visibility` en inputs
**Esfuerzo:** S | **Archivos:** `ui/inputs.py`, `ui/rewrite.py`
Mezcla actual de `collapsed`/`visible` da sensación de UI a medio terminar. Regla: `collapsed` solo cuando hay `placeholder=` descriptivo, `visible` si no. Aplicar en todos los `text_input`/`text_area`.

### P4.8 — Copy buttons también en panel de rewrite
**Esfuerzo:** XS | **Archivo:** `ui/rewrite.py` (panel de resultados)
`ui/results.py` tiene "📋 Copiar HTML" pero el panel de rewrite no. Replicar el componente con feedback de 2-3s.

### P4.9 — Mensajes de error con next-step accionable en GSC
**Esfuerzo:** S | **Archivos:** `ui/verify.py:83`, `ui/gsc_section.py:69-73`
"Error al verificar contenido. Revisa los logs" → bloquea flujo. Cambiar a "GSC no disponible. Continúa sin verificación" + botón directo a "Cargar CSV".

### P4.10 — Empty state con guía en modos sin datos
**Esfuerzo:** S | **Archivos:** `ui/opportunities.py`, `ui/verify.py`
Cuando GSC no tiene datos / CSV no cargado, el panel queda vacío. Añadir un empty state corto: icono + "Conecta GSC o sube un CSV para empezar" + link al expander de configuración.

### P4.11 — Versión visible en footer/sidebar
**Esfuerzo:** XS | **Archivo:** `app.py` footer
Hoy la versión sólo aparece en debug. Mostrarla en el footer (`v{__version__}`) facilita reportar bugs y verificar despliegues.

### P4.12 — Resumen de config aplicada al final del panel de resultados
**Esfuerzo:** S | **Archivo:** `ui/results.py`
Tras generar, mostrar un caption colapsable con: keyword, arquetipo, longitud objetivo vs real, visual elements usados, productos incluidos. Permite reproducir o iterar sin volver al formulario.

---

## Orden de ejecución sugerido

1. **P3.1** (min_score) — corrige distorsión de calidad **YA**
2. **P3.2, P3.4** — pérdida de datos en n8n
3. **P3.5** — eliminar scraper duplicado en rewrite (prerequisito de paridad)
4. **P3.3, P3.6, P3.7, P3.9** — robustez
5. **P4.1, P4.2, P4.3, P4.4, P4.5, P4.8, P4.11** — quick wins (XS)
6. **P4.6, P4.7, P4.9, P4.10, P4.12** — esfuerzo S
7. **P3.8** — verificación final (no acción, solo confirmar)

## Notas

- Los hallazgos del audit P3 que mencionaban `serp_research.py` no incluyen logs/raise_for_status fueron descartados — el código ya los tiene (`utils/serp_research.py:202-206`).
- El truncado de `rewrite.py:2036` opera sobre texto plano (`get_text`), no HTML, así que no rompe tags. Se mantiene como P3.6 sólo por pérdida de contexto.
- P4.7 y P4.10 son pequeños refactors de consistencia, no rediseños — mantener bajo el umbral de "polish".
